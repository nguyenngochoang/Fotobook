- current_user.followees_photos.each do |post|
	- if post.sharing_mode
		.col-12.col-lg-6
			.card
				.card-body
					.container
						.row
							.col-lg-6.col-12.py-0.img.photo data-id="#{post.id}" data-param="#{post.photoable_id}"
								= render partial: 'thumbnail',locals:{link: post.attached_image.url , mode:"photo"}
							.col-lg-6.col-12.content
								.userInfo
									= link_to root_path do
										= image_tag post.photoable.avatar.url,alt:"Card image",class:"card-img-top img-fluid userava rounded-circle"
										span.align-self-center.ml-1.text-black.font-weight-bolder
											= "#{post.photoable.first_name} #{post.photoable.last_name}"
								.row.row-text
									h5.card-title = post.title
									p = post.description
								.row.px-2
									hr/
									.heart-animation.d-inline-block.bot-element
									span.likesspan.bot-element-text = "#{post.like} Like"
									span.date = local_time(post.created_at)

